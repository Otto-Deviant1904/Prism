# QUICKSTART

1. Copy env:
```bash
cp .env.example .env
```

2. Start everything:
```bash
pnpm install
pnpm dev:full
```

3. Open:
- App: `http://localhost:3001`
- Admin: `http://localhost:3001/admin`

4. Demo query:
- `lumi cream`
